# ignore the user configuration file
conf.yaml

# 使用者的模型清單。app 會在使用者設定動作對應與點擊區時改寫它，所以它是
# 狀態不是隨附檔案；隨附的是 config_templates/model_dict.default.json，
# 首次讀取時複製過來。留在版控裡的話，每次在 UI 調設定都會冒出一筆 git
# 修改，順手 commit 就把本機的模型寫進 repo。
model_dict.json
# 設定檔的備份也是使用者的東西——而且會夾帶端點、路徑、金鑰欄位。
conf.yaml.bak*

# 模型製作的工作區。那是做 Live2D 的中間產物（PSD 拆層腳本、試驗版本、
# 上百 MB 的貼圖），跟執行這個 app 無關，而且往往牽涉到有版權的角色素材。
/assets/live2d-work/

# 只有已經追蹤的那批通用背景會跟著發佈。這行擋的是「之後放進來的」——
# .gitignore 管不到已追蹤的檔案，所以既有的背景不受影響，而你自己丟進
# backgrounds/ 的圖（含有版權角色場景）不會被 git add -A 掃進來。
# 真的要新增隨附背景時，用 git add -f 明確表態。
/backgrounds/*

# 只附帶 Live2D 官方的免費範例。任何有版權的角色美術（同人模型、動畫角色）
# 都不進版控——那是 NOTICE 裡「Not included — bring your own」那一條的實作，
# 不是偏好問題。自己本機要用什麼放進 live2d-models/ 就好，不會被追蹤。
# ignore non-default live2d models
/live2d-models/*
!/live2d-models/mao_pro
!/live2d-models/haru
!/live2d-models/hiyori

# 跟 live2d-models 同一條規則：只附帶授權明確、可再散布的範例，其餘都是使用者
# 本機資產。Sendagaya_Shino 的 VRM 檔案內嵌 licenseName: CC0、allowedUser:
# Everyone；動作片段來自 MIT 授權的 tk256ailab/vrm-viewer（授權與聲明附在
# motions/ 裡）；idle.vrma 是 scripts/make_idle_vrma.py 從零生成的。
# 三者都可以再散布。
/vrm-models/*
!/vrm-models/Sendagaya_Shino

# ignore non-default character configs
/characters/*
!/characters/README.md
/personas/*
!/personas/README.md
# but all of the defaults are already tracked, so no need to un-ignore them

# ignore avatars
avatars/*

# macOS system files
.DS_Store

# Python files
__pycache__/
*.pyc
lab.py
.idea

# Virtual environment
.venv
.conda
conda

# Sensitive data
.env
api_keys.py
src/open_llm_vtuber/llm/user_credentials.json

# Database files
memory.db
memory.db.bk

# Logs
server.log
logs/*
*.log

# Cache and models
cache/*
generated_images/
src/open_llm_vtuber/tts/asset
src/open_llm_vtuber/tts/config
src/open_llm_vtuber/asr/models*
!src/open_llm_vtuber/asr/models/silero_vad.onnx
asset
models/*
!models/piper_voice
models/piper_voice/*


# Legacy and specific directories
legacy/
chat_history/
knowledge_base/
submodules/MeloTTS
openapi_assistants.json
openapi_memgpt.json

# memory log
mem.json
conf.yaml.backup

*.exe

tmp/
private/
mcp_servers.json

.vscode/
# runtime artifacts
conf.yaml.bak
# 帶時戳的備份（character_route 與手動改設定時各會留一份）
conf.yaml.bak.*
*.bak
prompts/utils/proactive_topics.json
prompts/utils/seen_news.json
prompts/utils/proactive_speak_prompt.txt

# Electron 外殼

# 前端原始碼的建置產物
frontend-src/node_modules/
frontend-src/dist/
frontend-src/out/

# 設計文件與工作筆記。這是開發過程的東西，不是給使用者看的，而這個 repo 會公開。
# 注意：.gitignore 對已追蹤的檔案無效——真的不小心 commit 進去了，要一併
# git rm --cached，光加這一行沒有用。
docs/superpowers/

# 個人角色 rig 的工作用 skill。它的每個步驟都指向 assets/live2d-work/ 底下
# 沒進版控的有版權角色素材（SKILL.md 寫的是作者本機的角色模型），跟 NOTICE 的「bring your own，不要放有版權角色」相牴觸，對 clone
# 下來的人也沒有用——那些路徑在他們機器上不存在。留在本機就好。
/skills/
