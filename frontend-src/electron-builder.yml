appId: com.tomoshibi.app
productName: Tomoshibi
directories:
  buildResources: resources
  output: release/${version}
files:
  - 'dist/**/*'
  - 'out/**/*'
  - 'resources/**/*'
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
win:
  executableName: Tomoshibi
  icon: resources/icon.ico
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  differentialPackage: false
mac:
  icon: resources/icon.icns
  target:
    - target: dmg
      arch: [x64, arm64]
  # resources/ 而不是預設的 build/：build 在 .gitignore 裡（第 19 行），
  # 放那邊的話這個檔進不了版控，簽章時就會找不到。
  entitlements: resources/entitlements.mac.plist
  entitlementsInherit: resources/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
  writeUpdateInfo: false
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: Kuren Peng <weryk153@users.noreply.github.com>
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
publish:
  provider: github
  owner: weryk153
  repo: tomoshibi
  # draft，不是 release：打包時就算帶了 --publish，也只會產生草稿，
  # 要不要公開由人按下去決定。目前還沒有任何打包發行版。
  releaseType: draft
  publishAutoUpdate: false