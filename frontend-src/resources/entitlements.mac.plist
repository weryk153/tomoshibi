<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements for a hardened-runtime macOS build. electron-builder.yml points
  at this file via `mac.entitlementsInherit`, so it has to exist even while
  `notarize: false` and the app ships unsigned.

  The camera and microphone entries pair with the NSCameraUsageDescription /
  NSMicrophoneUsageDescription strings in electron-builder.yml: without both the
  entitlement and the usage string, a signed build is denied access silently.
  The JIT and unsigned-memory entries are what Electron's V8 needs under the
  hardened runtime.
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
  </dict>
</plist>
