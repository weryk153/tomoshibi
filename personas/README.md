# Persona presets

`personas.json` is created automatically by the app's **Settings → Personas**
screen. Persona presets contain only a name and a system prompt. They do not
duplicate or change Live2D models, voices, character identities, chat histories,
or long-term memories.

The selected persona is remembered independently for each character.
